<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.oditji.business.dao.BusinessDAO">

    <!-- =====================================================
         로그인 회원과 연결된 사업자 정보
    ====================================================== -->
    <select id="selectBusinessByMemberNo"
            parameterType="long"
            resultType="com.project.oditji.business.vo.BusinessVO">

        SELECT
            BUSINESS_NO,
            MEMBER_NO,
            BUSINESS_NAME,
            BUSINESS_NUMBER,
            BANK_NAME,
            ACCOUNT_NUMBER,
            ACCOUNT_HOLDER,
            GRADE_NAME,
            CUSTOM_RATE,
            STATUS,
            CREATED_AT
        FROM BUSINESS
        WHERE MEMBER_NO = #{memberNo}

    </select>


    <!-- =====================================================
         상품 등록
    ====================================================== -->
    <insert id="insertProduct"
            parameterType="com.project.oditji.business.vo.GoodsManageVO">

        <selectKey keyProperty="productNo"
                   resultType="long"
                   order="BEFORE">

            SELECT SEQ_PRODUCT_NO.NEXTVAL
            FROM DUAL

        </selectKey>

        INSERT INTO PRODUCT (
            PRODUCT_NO,
            BUSINESS_NO,
            CONTENT_NO,
            ACTOR_NO,
            PRODUCT_NAME,
            PRODUCT_TYPE,
            PRICE,
            DISCOUNT_RATE,
            STOCK,
            DESCRIPTION,
            STATUS,
            CREATED_AT
        )
        VALUES (
            #{productNo},
            #{businessNo},
            #{contentNo},
            #{actorNo, jdbcType=NUMERIC},
            #{productName},
            #{productType},
            #{price},
            #{discountRate},
            #{stock},
            #{description, jdbcType=CLOB},
            #{status},
            SYSDATE
        )

    </insert>


    <!-- =====================================================
         상품 대표 이미지 등록
    ====================================================== -->
    <insert id="insertProductImage"
            parameterType="com.project.oditji.business.vo.GoodsManageVO">

        <selectKey keyProperty="imageNo"
                   resultType="long"
                   order="BEFORE">

            SELECT SEQ_PRODUCT_IMAGE_NO.NEXTVAL
            FROM DUAL

        </selectKey>

        INSERT INTO PRODUCT_IMAGE (
            IMAGE_NO,
            PRODUCT_NO,
            IMAGE_PATH,
            IS_MAIN
        )
        VALUES (
            #{imageNo},
            #{productNo},
            #{imagePath},
            #{isMain}
        )

    </insert>


    <!-- =====================================================
         콘텐츠 검색
    ====================================================== -->
    <select id="selectContentList"
            resultType="com.project.oditji.business.vo.ContentSearchVO">

        SELECT
            CONTENT_NO,
            TMDB_ID,
            CONTENT_TYPE,
            TITLE,
            ORIGINAL_TITLE,
            POSTER_PATH,
            RELEASE_DATE,
            GENRE_TEXT,
            AGE_RATING
        FROM CONTENT
        WHERE 1 = 1

        <if test="keyword != null and keyword != ''">

            AND (
                UPPER(TITLE)
                    LIKE '%' || UPPER(#{keyword}) || '%'

                OR UPPER(ORIGINAL_TITLE)
                    LIKE '%' || UPPER(#{keyword}) || '%'
            )

        </if>

        ORDER BY CONTENT_NO DESC

    </select>


    <!-- =====================================================
         콘텐츠 단건 조회
    ====================================================== -->
    <select id="selectContentByNo"
            parameterType="long"
            resultType="com.project.oditji.business.vo.ContentSearchVO">

        SELECT
            CONTENT_NO,
            TMDB_ID,
            CONTENT_TYPE,
            TITLE,
            ORIGINAL_TITLE,
            POSTER_PATH,
            RELEASE_DATE,
            GENRE_TEXT,
            AGE_RATING
        FROM CONTENT
        WHERE CONTENT_NO = #{contentNo}

    </select>


    <!-- =====================================================
         전체 배우 목록
    ====================================================== -->
    <select id="selectActorList"
            resultType="com.project.oditji.business.vo.ActorSearchVO">

        SELECT
            ACTOR_NO,
            TMDB_ACTOR_ID,
            ACTOR_NAME,
            PROFILE_PATH
        FROM ACTOR
        ORDER BY ACTOR_NAME ASC

    </select>


    <!-- =====================================================
         선택한 콘텐츠에 연결된 배우 목록
    ====================================================== -->
    <select id="selectActorListByContentNo"
            parameterType="long"
            resultType="com.project.oditji.business.vo.ActorSearchVO">

        SELECT
            A.ACTOR_NO,
            A.TMDB_ACTOR_ID,
            A.ACTOR_NAME,
            A.PROFILE_PATH,
            CA.CHARACTER_NAME,
            CA.DISPLAY_ORDER
        FROM CONTENT_ACTOR CA

        JOIN ACTOR A
          ON A.ACTOR_NO = CA.ACTOR_NO

        WHERE CA.CONTENT_NO = #{contentNo}

        ORDER BY
            CA.DISPLAY_ORDER ASC NULLS LAST,
            A.ACTOR_NAME ASC

    </select>


    <!-- =====================================================
         선택한 콘텐츠와 배우의 연결 여부 검증
    ====================================================== -->
    <select id="countContentActor"
            resultType="int">

        SELECT COUNT(*)
        FROM CONTENT_ACTOR
        WHERE CONTENT_NO = #{contentNo}
          AND ACTOR_NO = #{actorNo}

    </select>


    <!-- =====================================================
         사업자가 등록한 상품 목록
    ====================================================== -->
    <select id="selectProductListByBusinessNo"
            parameterType="long"
            resultType="com.project.oditji.business.vo.GoodsManageVO">

        SELECT
            P.PRODUCT_NO,
            P.BUSINESS_NO,
            P.CONTENT_NO,
            P.ACTOR_NO,
            P.PRODUCT_NAME,
            P.PRODUCT_TYPE,
            P.PRICE,
            P.DISCOUNT_RATE,
            P.STOCK,
            P.DESCRIPTION,
            P.STATUS,
            P.CREATED_AT,

            C.TITLE AS CONTENT_TITLE,
            A.ACTOR_NAME,
            B.BUSINESS_NAME,

            (
                SELECT PI.IMAGE_PATH
                FROM PRODUCT_IMAGE PI
                WHERE PI.PRODUCT_NO = P.PRODUCT_NO
                  AND PI.IS_MAIN = 'Y'
                  AND ROWNUM = 1
            ) AS IMAGE_PATH

        FROM PRODUCT P

        JOIN BUSINESS B
          ON B.BUSINESS_NO = P.BUSINESS_NO

        JOIN CONTENT C
          ON C.CONTENT_NO = P.CONTENT_NO

        LEFT JOIN ACTOR A
          ON A.ACTOR_NO = P.ACTOR_NO

        WHERE P.BUSINESS_NO = #{businessNo}

        ORDER BY P.PRODUCT_NO DESC

    </select>


    <!-- =====================================================
         상품 수정 화면용 상품 단건 조회

         상품 번호와 사업자 번호를 함께 조회하여
         다른 사업자의 상품을 수정할 수 없도록 한다.
    ====================================================== -->
    <select id="selectProductForUpdate"
            resultType="com.project.oditji.business.vo.GoodsManageVO">

        SELECT
            P.PRODUCT_NO,
            P.BUSINESS_NO,
            P.CONTENT_NO,
            P.ACTOR_NO,
            P.PRODUCT_NAME,
            P.PRODUCT_TYPE,
            P.PRICE,
            P.DISCOUNT_RATE,
            P.STOCK,
            P.DESCRIPTION,
            P.STATUS,
            P.CREATED_AT,

            C.TITLE AS CONTENT_TITLE,
            A.ACTOR_NAME,
            B.BUSINESS_NAME,

            (
                SELECT PI.IMAGE_NO
                FROM PRODUCT_IMAGE PI
                WHERE PI.PRODUCT_NO = P.PRODUCT_NO
                  AND PI.IS_MAIN = 'Y'
                  AND ROWNUM = 1
            ) AS IMAGE_NO,

            (
                SELECT PI.IMAGE_PATH
                FROM PRODUCT_IMAGE PI
                WHERE PI.PRODUCT_NO = P.PRODUCT_NO
                  AND PI.IS_MAIN = 'Y'
                  AND ROWNUM = 1
            ) AS IMAGE_PATH,

            'Y' AS IS_MAIN

        FROM PRODUCT P

        JOIN BUSINESS B
          ON B.BUSINESS_NO = P.BUSINESS_NO

        JOIN CONTENT C
          ON C.CONTENT_NO = P.CONTENT_NO

        LEFT JOIN ACTOR A
          ON A.ACTOR_NO = P.ACTOR_NO

        WHERE P.PRODUCT_NO = #{productNo}
          AND P.BUSINESS_NO = #{businessNo}

    </select>


    <!-- =====================================================
         상품 기본 정보 수정

         상품 수정 요청 이후에는 관리자 재승인을 받을 수 있도록
         STATUS를 WAITING으로 변경한다.
    ====================================================== -->
    <update id="updateProduct"
            parameterType="com.project.oditji.business.vo.GoodsManageVO">

        UPDATE PRODUCT
        SET
            CONTENT_NO = #{contentNo},
            ACTOR_NO = #{actorNo, jdbcType=NUMERIC},
            PRODUCT_NAME = #{productName},
            PRODUCT_TYPE = #{productType},
            PRICE = #{price},
            DISCOUNT_RATE = #{discountRate},
            STOCK = #{stock},
            DESCRIPTION = #{description, jdbcType=CLOB},
            STATUS = #{status}
        WHERE PRODUCT_NO = #{productNo}
          AND BUSINESS_NO = #{businessNo}

    </update>


    <!-- =====================================================
         기존 대표 이미지 경로 수정
    ====================================================== -->
    <update id="updateProductMainImage"
            parameterType="com.project.oditji.business.vo.GoodsManageVO">

        UPDATE PRODUCT_IMAGE
        SET IMAGE_PATH = #{imagePath}
        WHERE PRODUCT_NO = #{productNo}
          AND IS_MAIN = 'Y'

    </update>


    <!-- =====================================================
         상품 삭제 요청

         상품 데이터를 실제 삭제하지 않고
         관리자 확인 전 상태값만 DELETE_REQUESTED로 변경한다.

         이미 삭제 요청된 상품은 다시 변경하지 않는다.
    ====================================================== -->
    <update id="updateProductDeleteRequest">

        UPDATE PRODUCT
        SET STATUS = 'DELETE_REQUESTED'
        WHERE PRODUCT_NO = #{productNo}
          AND BUSINESS_NO = #{businessNo}
          AND STATUS != 'DELETE_REQUESTED'

    </update>

</mapper>