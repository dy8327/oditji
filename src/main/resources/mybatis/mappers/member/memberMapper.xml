<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.oditji.member.dao.MemberDAO">

    <insert id="insertMember" parameterType="com.project.oditji.member.vo.MemberVO">

        <selectKey keyProperty="memberNo" resultType="long" order="BEFORE">
            SELECT SEQ_MEMBER_NO.NEXTVAL
            FROM DUAL
        </selectKey>

        INSERT INTO MEMBER (
            MEMBER_NO,
            MEMBER_ID,
            MEMBER_PW,
            MEMBER_NAME,
            NICKNAME,
            EMAIL,
            PHONE,
            PROFILE_IMAGE,
            ROLE,
            STATUS,
            ADULT_VERIFIED,
            CREATED_AT
        ) VALUES (
            #{memberNo},
            #{memberId, jdbcType=VARCHAR},
            #{memberPw, jdbcType=VARCHAR},
            #{memberName, jdbcType=VARCHAR},
            #{nickname, jdbcType=VARCHAR},
            #{email, jdbcType=VARCHAR},
            #{phone, jdbcType=VARCHAR},
            #{profileImage, jdbcType=VARCHAR},
            #{role, jdbcType=VARCHAR},
            #{status, jdbcType=VARCHAR},
            #{adultVerified, jdbcType=VARCHAR},
            SYSDATE
        )
    </insert>

    <insert id="insertMemberPlatform">
        INSERT INTO MEMBER_PLATFORM (
            MEMBER_PLATFORM_NO,
            MEMBER_NO,
            PLATFORM_NO,
            CREATED_AT
        ) VALUES (
            SEQ_MEMBER_PLATFORM_NO.NEXTVAL,
            #{memberNo},
            #{platformNo},
            SYSDATE
        )
    </insert>

    <resultMap id="memberResultMap" type="com.project.oditji.member.vo.MemberVO">
        <id property="memberNo" column="MEMBER_NO"/>
        <result property="memberId" column="MEMBER_ID"/>
        <result property="memberPw" column="MEMBER_PW"/>
        <result property="memberName" column="MEMBER_NAME"/>
        <result property="nickname" column="NICKNAME"/>
        <result property="email" column="EMAIL"/>
        <result property="phone" column="PHONE"/>
        <result property="profileImage" column="PROFILE_IMAGE"/>
        <result property="role" column="ROLE"/>
        <result property="status" column="STATUS"/>
        <result property="adultVerified" column="ADULT_VERIFIED"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="updatedAt" column="UPDATED_AT"/>
        <result property="withdrawnAt" column="WITHDRAWN_AT"/>
    </resultMap>

    <!--
        로그인 조회
        - 정지(BLOCKED)/탈퇴(WITHDRAWN) 여부는 서비스 레이어에서 STATUS로 판단하므로
          WHERE절에 STATUS 조건을 걸지 않는다.
        - 탈퇴 회원 복구 화면에서 "복구 가능 기한"을 보여주기 위해 WITHDRAWN_AT을 반드시 조회한다.
    -->
    <select id="loginMember"
        parameterType="com.project.oditji.member.vo.MemberVO"
        resultMap="memberResultMap">

        SELECT
            MEMBER_NO,
            MEMBER_ID,
            MEMBER_PW,
            MEMBER_NAME,
            NICKNAME,
            EMAIL,
            PHONE,
            PROFILE_IMAGE,
            ROLE,
            STATUS,
            ADULT_VERIFIED,
            CREATED_AT,
            UPDATED_AT,
            WITHDRAWN_AT
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
        AND MEMBER_PW = #{memberPw}

    </select>

    <select id="countByMemberId" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
    </select>

    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE EMAIL = #{email}
    </select>

    <select id="countByNickname" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE NICKNAME = #{nickname}
    </select>

    <select id="selectPlatformNoByCode" parameterType="string" resultType="long">
        SELECT PLATFORM_NO
        FROM OTT_PLATFORM
        WHERE UPPER(TRIM(PLATFORM_NAME)) = UPPER(TRIM(#{platformCode}))
    </select>

    <insert id="insertKakaoMember" parameterType="com.project.oditji.member.vo.MemberVO">

        <selectKey keyProperty="memberNo" resultType="long" order="BEFORE">
            SELECT SEQ_MEMBER_NO.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO MEMBER (
            MEMBER_NO,
            MEMBER_ID,
            MEMBER_PW,
            MEMBER_NAME,
            NICKNAME,
            EMAIL,
            PHONE,
            PROFILE_IMAGE,
            ROLE,
            STATUS,
            ADULT_VERIFIED,
            CREATED_AT
        ) VALUES (
            #{memberNo},
            #{memberId, jdbcType=VARCHAR},
            #{memberPw, jdbcType=VARCHAR},
            #{memberName, jdbcType=VARCHAR},
            #{nickname, jdbcType=VARCHAR},
            #{email, jdbcType=VARCHAR},
            #{phone, jdbcType=VARCHAR},
            #{profileImage, jdbcType=VARCHAR},
            'USER',
            'ACTIVE',
            'N',
            SYSDATE
        )
    </insert>

    <delete id="deleteMemberPlatform" parameterType="long">
        DELETE FROM MEMBER_PLATFORM
        WHERE MEMBER_NO = #{memberNo}
    </delete>

    <!--
        회원 마이페이지 자진 탈퇴 처리
        - 즉시 삭제하지 않고 STATUS만 WITHDRAWN으로 변경, WITHDRAWN_AT을 기록한다.
        - 실제 삭제는 MemberDeleteScheduler -> AdminService.deleteExpiredWithdrawMembers()가
          WITHDRAWN_AT 기준 7일 경과 후 처리한다.
    -->
    <update id="withdrawMember" parameterType="long">
        UPDATE MEMBER
        SET STATUS = 'WITHDRAWN',
            WITHDRAWN_AT = SYSDATE,
            UPDATED_AT = SYSDATE
        WHERE MEMBER_NO = #{memberNo}
    </update>

    <!--
        탈퇴 회원 복구 처리
        - STATUS가 WITHDRAWN이고, WITHDRAWN_AT 기준 7일 이내인 경우에만 ACTIVE로 되돌린다.
        - 7일 조건은 MemberDeleteScheduler의 하드 삭제와의 레이스 컨디션을 막기 위한
          방어적 이중 체크이다 (서비스 레이어에서도 별도 검증하지 않고 이 쿼리의 갱신 row 수로 판단한다).
        - 비밀번호는 재확인하지 않는다. 이 쿼리를 호출하는 시점(MemberServiceImpl.restoreMember)은
          이미 로그인/카카오 콜백에서 본인 인증이 끝난 뒤이다.
    -->
    <update id="restoreMember" parameterType="long">
        UPDATE MEMBER
        SET STATUS = 'ACTIVE',
            WITHDRAWN_AT = NULL,
            UPDATED_AT = SYSDATE
        WHERE MEMBER_NO = #{memberNo}
          AND STATUS = 'WITHDRAWN'
          AND WITHDRAWN_AT >= SYSDATE - 7
    </update>

    <select id="checkPassword" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE MEMBER_NO = #{memberNo}
        AND TRIM(MEMBER_PW) = TRIM(#{password})
    </select>

    <select id="getMemberByNo"
            parameterType="long"
            resultMap="memberResultMap">

        SELECT
            MEMBER_NO,
            MEMBER_ID,
            MEMBER_PW,
            MEMBER_NAME,
            NICKNAME,
            EMAIL,
            PHONE,
            PROFILE_IMAGE,
            ROLE,
            STATUS,
            ADULT_VERIFIED,
            CREATED_AT,
            UPDATED_AT,
            WITHDRAWN_AT
        FROM MEMBER
        WHERE MEMBER_NO = #{memberNo}

    </select>

    <select id="countByNicknameExceptMe" resultType="int">

        SELECT COUNT(*)
        FROM MEMBER
        WHERE NICKNAME = #{nickname}
        AND MEMBER_NO != #{memberNo}

    </select>

    <update id="updateMember"
            parameterType="com.project.oditji.member.vo.MemberVO">

        UPDATE MEMBER
        SET
            NICKNAME = #{nickname},
            EMAIL = NVL(#{email, jdbcType=VARCHAR}, EMAIL),
            PHONE = #{phone},
            MEMBER_PW = NVL(#{memberPw, jdbcType=VARCHAR}, MEMBER_PW),
            PROFILE_IMAGE = NVL(#{profileImage, jdbcType=VARCHAR}, PROFILE_IMAGE),
            UPDATED_AT = SYSDATE
        WHERE MEMBER_NO = #{memberNo}

    </update>

    <select id="findId" resultType="com.project.oditji.member.vo.MemberVO">

        SELECT MEMBER_ID
        FROM MEMBER
        WHERE MEMBER_NAME = #{memberName}
        AND EMAIL = #{email}

    </select>

    <select id="findPw" resultType="com.project.oditji.member.vo.MemberVO">

        SELECT MEMBER_PW
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
        AND MEMBER_NAME = #{memberName}
        AND EMAIL = #{email}

    </select>

</mapper>
