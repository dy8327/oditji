<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.oditji.event.dao.OttDiscountDAO">

    <resultMap id="ottDiscountResultMap" type="com.project.oditji.event.vo.OttDiscountVO">
        <id property="discountId" column="DISCOUNT_ID"/>
        <result property="platformCode" column="PLATFORM_CODE"/>
        <result property="platformName" column="PLATFORM_NAME"/>
        <result property="category" column="CATEGORY"/>
        <result property="title" column="TITLE"/>
        <result property="discountSummary" column="DISCOUNT_SUMMARY"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="cardOrCompany" column="CARD_OR_COMPANY"/>
        <result property="targetUrl" column="TARGET_URL"/>
        <result property="badgeText" column="BADGE_TEXT"/>
        <result property="startDate" column="START_DATE"/>
        <result property="endDate" column="END_DATE"/>
        <result property="isActive" column="IS_ACTIVE"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="regularPrice" column="REGULAR_PRICE"/>
        <result property="discountPrice" column="DISCOUNT_PRICE"/>
    </resultMap>

    <sql id="discountColumns">
        DISCOUNT_ID,
        PLATFORM_CODE,
        PLATFORM_NAME,
        CATEGORY,
        TITLE,
        DISCOUNT_SUMMARY,
        DESCRIPTION,
        CARD_OR_COMPANY,
        TARGET_URL,
        BADGE_TEXT,
        START_DATE,
        END_DATE,
        IS_ACTIVE,
        CREATED_AT,
        REGULAR_PRICE,
        DISCOUNT_PRICE
    </sql>

    <!-- 사용자용 목록 조회 공통 조건 (필터 + 활성/기간). 목록/건수 조회가 동일한 조건을 써야
         페이지 수 계산이 어긋나지 않으므로 sql 조각으로 분리해 재사용한다. -->
    <sql id="discountListWhere">
        WHERE IS_ACTIVE = 'Y'
          AND (START_DATE IS NULL OR START_DATE &lt;= TO_CHAR(SYSDATE, 'YYYY-MM-DD'))
          AND (END_DATE IS NULL OR END_DATE &gt;= TO_CHAR(SYSDATE, 'YYYY-MM-DD'))
        <if test="platform != null and platform != '' and platform != 'ALL'">
            AND PLATFORM_CODE = #{platform}
        </if>
        <if test="category != null and category != '' and category != 'ALL'">
            AND CATEGORY = #{category}
        </if>
    </sql>

    <!-- 사용자용 목록 조회: 활성화 상태 및 기간 조건 적용, 9개 단위 페이징(OFFSET/FETCH)
         IS_ACTIVE는 CHAR(1) 'Y'/'N' 컬럼이므로 문자 리터럴로 비교한다. -->
    <select id="selectDiscountList" resultMap="ottDiscountResultMap">
        SELECT
            <include refid="discountColumns"/>
        FROM OTT_DISCOUNT_INFO
        <include refid="discountListWhere"/>
        ORDER BY DISCOUNT_ID DESC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
    </select>

    <!-- 사용자용 목록 전체 건수 조회 (페이지 수 계산용) -->
    <select id="selectDiscountListCount" resultType="int">
        SELECT COUNT(*)
        FROM OTT_DISCOUNT_INFO
        <include refid="discountListWhere"/>
    </select>

    <!-- [수정] 상단 히어로 배너용 1건 조회. BEST 뱃지가 있으면 그걸, 없으면 최신 항목을
         페이징과 무관하게 항상 정확히 가져온다(목록이 9개씩 잘려도 영향받지 않음). -->
    <select id="selectHeroDiscount" resultMap="ottDiscountResultMap">
        SELECT
            <include refid="discountColumns"/>
        FROM OTT_DISCOUNT_INFO
        <include refid="discountListWhere"/>
        ORDER BY CASE WHEN BADGE_TEXT = 'BEST' THEN 0 ELSE 1 END, DISCOUNT_ID DESC
        FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- 관리자용 목록 조회: IS_ACTIVE 필터 없이 조회하되, status가 Y/N이면 해당 상태만 조회 -->
    <select id="selectAdminDiscountList" resultMap="ottDiscountResultMap">
        SELECT
            <include refid="discountColumns"/>
        FROM OTT_DISCOUNT_INFO
        <where>
            <if test="platform != null and platform != '' and platform != 'ALL'">
                AND PLATFORM_CODE = #{platform}
            </if>
            <if test="category != null and category != '' and category != 'ALL'">
                AND CATEGORY = #{category}
            </if>
            <if test='status != null and (status == "Y" or status == "N")'>
                AND IS_ACTIVE = #{status}
            </if>
        </where>
        ORDER BY DISCOUNT_ID DESC
    </select>

    <!-- 단건 조회 -->
    <select id="selectDiscountById" parameterType="long" resultMap="ottDiscountResultMap">
        SELECT
            <include refid="discountColumns"/>
        FROM OTT_DISCOUNT_INFO
        WHERE DISCOUNT_ID = #{discountId}
    </select>

    <!-- 등록 -->
    <insert id="insertDiscount" parameterType="com.project.oditji.event.vo.OttDiscountVO">
        <selectKey keyProperty="discountId" resultType="long" order="BEFORE">
            SELECT SEQ_OTT_DISCOUNT_NO.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO OTT_DISCOUNT_INFO (
            DISCOUNT_ID,
            PLATFORM_CODE,
            PLATFORM_NAME,
            CATEGORY,
            TITLE,
            DISCOUNT_SUMMARY,
            DESCRIPTION,
            CARD_OR_COMPANY,
            TARGET_URL,
            BADGE_TEXT,
            START_DATE,
            END_DATE,
            IS_ACTIVE,
            CREATED_AT,
            REGULAR_PRICE,
            DISCOUNT_PRICE
        ) VALUES (
            #{discountId},
            #{platformCode},
            #{platformName},
            #{category},
            #{title},
            #{discountSummary},
            #{description, jdbcType=VARCHAR},
            #{cardOrCompany, jdbcType=VARCHAR},
            #{targetUrl, jdbcType=VARCHAR},
            #{badgeText, jdbcType=VARCHAR},
            #{startDate, jdbcType=VARCHAR},
            #{endDate, jdbcType=VARCHAR},
            NVL(#{isActive, jdbcType=VARCHAR}, 'Y'),
            SYSDATE,
            #{regularPrice, jdbcType=INTEGER},
            #{discountPrice, jdbcType=INTEGER}
        )
    </insert>

    <!-- 수정 -->
    <update id="updateDiscount" parameterType="com.project.oditji.event.vo.OttDiscountVO">
        UPDATE OTT_DISCOUNT_INFO
        SET
            PLATFORM_CODE = #{platformCode},
            PLATFORM_NAME = #{platformName},
            CATEGORY = #{category},
            TITLE = #{title},
            DISCOUNT_SUMMARY = #{discountSummary},
            DESCRIPTION = #{description, jdbcType=VARCHAR},
            CARD_OR_COMPANY = #{cardOrCompany, jdbcType=VARCHAR},
            TARGET_URL = #{targetUrl, jdbcType=VARCHAR},
            BADGE_TEXT = #{badgeText, jdbcType=VARCHAR},
            START_DATE = #{startDate, jdbcType=VARCHAR},
            END_DATE = #{endDate, jdbcType=VARCHAR},
            IS_ACTIVE = NVL(#{isActive, jdbcType=VARCHAR}, IS_ACTIVE),
            REGULAR_PRICE = #{regularPrice, jdbcType=INTEGER},
            DISCOUNT_PRICE = #{discountPrice, jdbcType=INTEGER}
        WHERE DISCOUNT_ID = #{discountId}
    </update>

    <!-- 삭제(비활성화) : 서비스 계층에서 "소프트 삭제"로 안내하고 있고, 재활성화(activateDiscount)
         API가 별도로 존재하므로 물리 삭제가 아닌 IS_ACTIVE='N' 처리로 구현한다. -->
    <update id="deleteDiscount" parameterType="long">
        UPDATE OTT_DISCOUNT_INFO
        SET IS_ACTIVE = 'N'
        WHERE DISCOUNT_ID = #{discountId}
    </update>

    <!-- 비활성화된 할인 정보 재활성화 -->
    <update id="activateDiscount">
        UPDATE OTT_DISCOUNT_INFO
        SET IS_ACTIVE = 'Y'
        WHERE DISCOUNT_ID = #{discountId}
    </update>

    <!-- 만료된 할인 비활성화 -->
    <update id="updateExpiredDiscounts">
        UPDATE OTT_DISCOUNT_INFO
        SET IS_ACTIVE = 'N'
        WHERE END_DATE &lt; TO_CHAR(SYSDATE, 'YYYY-MM-DD')
          AND IS_ACTIVE = 'Y'
    </update>

</mapper>
