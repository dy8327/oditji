<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.oditji.content.dao.ContentDAO">

    <select id="selectContentByTmdbId"
            parameterType="com.project.oditji.content.vo.ContentVO"
            resultType="com.project.oditji.content.vo.ContentVO">

        SELECT
            CONTENT_NO AS contentNo,
            TMDB_ID AS tmdbId,
            CONTENT_TYPE AS contentType,
            TITLE AS title,
            ORIGINAL_TITLE AS originalTitle,
            OVERVIEW AS overview,
            POSTER_PATH AS posterPath,
            BACKDROP_PATH AS backdropPath,
            RELEASE_DATE AS releaseDate,
            GENRE_TEXT AS genreText,
            RUNTIME AS runtime,
            EPISODE_COUNT AS episodeCount,
            DIRECTOR AS director,
            CAST_NAMES AS castNames,
            AGE_RATING AS ageRating,
            TMDB_SCORE AS tmdbScore,
            VIEW_COUNT AS viewCount,
            CREATED_AT AS createdAt,
            UPDATED_AT AS updatedAt
        FROM CONTENT
        WHERE TMDB_ID = #{tmdbId}
          AND CONTENT_TYPE = #{contentType}

    </select>


    <select id="selectContentByContentNo"
            parameterType="int"
            resultType="com.project.oditji.content.vo.ContentVO">

        SELECT
            CONTENT_NO AS contentNo,
            TMDB_ID AS tmdbId,
            CONTENT_TYPE AS contentType,
            TITLE AS title,
            ORIGINAL_TITLE AS originalTitle,
            OVERVIEW AS overview,
            POSTER_PATH AS posterPath,
            BACKDROP_PATH AS backdropPath,
            RELEASE_DATE AS releaseDate,
            GENRE_TEXT AS genreText,
            RUNTIME AS runtime,
            EPISODE_COUNT AS episodeCount,
            DIRECTOR AS director,
            CAST_NAMES AS castNames,
            AGE_RATING AS ageRating,
            TMDB_SCORE AS tmdbScore,
            VIEW_COUNT AS viewCount,
            CREATED_AT AS createdAt,
            UPDATED_AT AS updatedAt
        FROM CONTENT
        WHERE CONTENT_NO = #{contentNo}

    </select>


    <insert id="insertContent"
            parameterType="com.project.oditji.content.vo.ContentVO">

        INSERT INTO CONTENT (
            CONTENT_NO,
            TMDB_ID,
            CONTENT_TYPE,
            TITLE,
            ORIGINAL_TITLE,
            OVERVIEW,
            POSTER_PATH,
            BACKDROP_PATH,
            RELEASE_DATE,
            GENRE_TEXT,
            RUNTIME,
            EPISODE_COUNT,
            DIRECTOR,
            CAST_NAMES,
            AGE_RATING,
            TMDB_SCORE,
            VIEW_COUNT,
            CREATED_AT
        )
        VALUES (
            SEQ_CONTENT_NO.NEXTVAL,
            #{tmdbId},
            #{contentType},
            #{title},
            #{originalTitle, jdbcType=VARCHAR},
            #{overview, jdbcType=CLOB},
            #{posterPath, jdbcType=VARCHAR},
            #{backdropPath, jdbcType=VARCHAR},
            #{releaseDate, jdbcType=DATE},
            #{genreText, jdbcType=VARCHAR},
            #{runtime, jdbcType=NUMERIC},
            #{episodeCount, jdbcType=NUMERIC},
            #{director, jdbcType=VARCHAR},
            #{castNames, jdbcType=VARCHAR},
            #{ageRating, jdbcType=VARCHAR},
            #{tmdbScore, jdbcType=NUMERIC},
            0,
            SYSDATE
        )

    </insert>


    <!--
        검색 JSON 캐시 값을 기존 CONTENT 행에 반영합니다.
        상세페이지 전용 값은 유지하고 검색 기준 값만 갱신합니다.
    -->
    <update id="updateContentFromSearchCache"
            parameterType="com.project.oditji.content.vo.ContentVO">

        UPDATE CONTENT
        SET
            TITLE = #{title},
            ORIGINAL_TITLE = #{originalTitle, jdbcType=VARCHAR},
            POSTER_PATH = #{posterPath, jdbcType=VARCHAR},
            RELEASE_DATE = #{releaseDate, jdbcType=DATE},
            GENRE_TEXT = #{genreText, jdbcType=VARCHAR},
            EPISODE_COUNT = #{episodeCount, jdbcType=NUMERIC},
            DIRECTOR = #{director, jdbcType=VARCHAR},
            CAST_NAMES = #{castNames, jdbcType=VARCHAR},
            AGE_RATING = #{ageRating, jdbcType=VARCHAR},
            TMDB_SCORE = #{tmdbScore, jdbcType=NUMERIC},
            UPDATED_AT = SYSDATE
        WHERE CONTENT_NO = #{contentNo}

    </update>


    <!-- 기존 CONTENT 전체 조회수 증가 -->
    <update id="increaseViewCount"
            parameterType="int">

        UPDATE CONTENT
        SET
            VIEW_COUNT = VIEW_COUNT + 1,
            UPDATED_AT = SYSDATE
        WHERE CONTENT_NO = #{contentNo}

    </update>


    <!--
        로그인 회원의 오늘 콘텐츠 상세 조회 이력을 저장합니다.

        같은 회원 + 같은 콘텐츠 + 같은 날짜의 기록이 있으면:
        - VIEW_COUNT 증가
        - LAST_VIEWED_AT 갱신

        오늘 기록이 없으면:
        - 새로운 행 등록
    -->
    <update id="mergeContentViewHistory"
            parameterType="com.project.oditji.content.vo.ContentViewHistoryVO">

        MERGE INTO CONTENT_VIEW_HISTORY TARGET
        USING (
            SELECT
                #{memberNo} AS MEMBER_NO,
                #{contentNo} AS CONTENT_NO,
                TRUNC(SYSDATE) AS VIEW_DATE
            FROM DUAL
        ) SOURCE
        ON (
            TARGET.MEMBER_NO = SOURCE.MEMBER_NO
            AND TARGET.CONTENT_NO = SOURCE.CONTENT_NO
            AND TARGET.VIEW_DATE = SOURCE.VIEW_DATE
        )

        WHEN MATCHED THEN
            UPDATE SET
                TARGET.VIEW_COUNT =
                    TARGET.VIEW_COUNT + 1,
                TARGET.LAST_VIEWED_AT =
                    SYSDATE

        WHEN NOT MATCHED THEN
            INSERT (
                VIEW_HISTORY_NO,
                MEMBER_NO,
                CONTENT_NO,
                VIEW_COUNT,
                VIEW_DATE,
                FIRST_VIEWED_AT,
                LAST_VIEWED_AT
            )
            VALUES (
                SEQ_CONTENT_VIEW_HISTORY_NO.NEXTVAL,
                SOURCE.MEMBER_NO,
                SOURCE.CONTENT_NO,
                1,
                SOURCE.VIEW_DATE,
                SYSDATE,
                SYSDATE
            )

    </update>


    <!--
        오늘을 포함한 최근 30일 기록만 유지합니다.

        예:
        오늘이 7월 23일이면
        6월 24일부터 7월 23일까지의 기록을 유지합니다.
    -->
    <delete id="deleteExpiredContentViewHistory">

        DELETE FROM CONTENT_VIEW_HISTORY
        WHERE VIEW_DATE <![CDATA[<]]>
              TRUNC(SYSDATE) - 29

    </delete>


    <!--
        메인 화면 콘텐츠 리스트 조회
        TMDB 평점과 전체 조회수를 기준으로 정렬합니다.
    -->
    <select id="selectMainContentList"
            resultType="com.project.oditji.content.vo.ContentVO">

        SELECT
            CONTENT_NO AS contentNo,
            TMDB_ID AS tmdbId,
            CONTENT_TYPE AS contentType,
            TITLE AS title,
            ORIGINAL_TITLE AS originalTitle,
            OVERVIEW AS overview,
            POSTER_PATH AS posterPath,
            BACKDROP_PATH AS backdropPath,
            RELEASE_DATE AS releaseDate,
            GENRE_TEXT AS genreText,
            RUNTIME AS runtime,
            EPISODE_COUNT AS episodeCount,
            DIRECTOR AS director,
            CAST_NAMES AS castNames,
            AGE_RATING AS ageRating,
            TMDB_SCORE AS tmdbScore,
            VIEW_COUNT AS viewCount,
            CREATED_AT AS createdAt,
            UPDATED_AT AS updatedAt
        FROM CONTENT
        ORDER BY
            TMDB_SCORE DESC,
            VIEW_COUNT DESC
        FETCH FIRST 200 ROWS ONLY

    </select>


    <select id="selectContentListByType"
            parameterType="map"
            resultType="com.project.oditji.content.vo.ContentVO">

        SELECT *
        FROM CONTENT

        ORDER BY
        <choose>
            <when test="type == 'new'">
                RELEASE_DATE DESC
            </when>

            <when test="type == 'popular'">
                VIEW_COUNT DESC
            </when>

            <otherwise>
                TMDB_SCORE DESC,
                VIEW_COUNT DESC
            </otherwise>
        </choose>

        OFFSET #{offset} ROWS
        FETCH FIRST #{size} ROWS ONLY

    </select>


    <select id="selectActorListByContentNo"
            parameterType="int"
            resultType="com.project.oditji.tmdb.vo.ActorVO">

        SELECT
            A.ACTOR_NO AS actorNo,
            A.TMDB_ACTOR_ID AS tmdbActorId,
            A.ACTOR_NAME AS actorName,
            A.PROFILE_PATH AS profilePath,
            CA.CHARACTER_NAME AS characterName,
            CA.DISPLAY_ORDER AS displayOrder
        FROM CONTENT_ACTOR CA
        JOIN ACTOR A
          ON A.ACTOR_NO = CA.ACTOR_NO
        WHERE CA.CONTENT_NO = #{contentNo}
        ORDER BY
            CA.DISPLAY_ORDER,
            A.ACTOR_NO

    </select>


    <select id="selectDirectorListByContentNo"
            parameterType="int"
            resultType="com.project.oditji.tmdb.vo.DirectorVO">

        SELECT
            D.DIRECTOR_NO AS directorNo,
            D.TMDB_DIRECTOR_ID AS tmdbDirectorId,
            D.DIRECTOR_NAME AS directorName,
            D.PROFILE_PATH AS profilePath,
            CD.DIRECTOR_TYPE AS directorType,
            CD.DISPLAY_ORDER AS displayOrder
        FROM CONTENT_DIRECTOR CD
        JOIN DIRECTOR D
          ON D.DIRECTOR_NO = CD.DIRECTOR_NO
        WHERE CD.CONTENT_NO = #{contentNo}
        ORDER BY
            CD.DISPLAY_ORDER,
            D.DIRECTOR_NO

    </select>


    <!-- 활성화된 콘텐츠별 시청 가능 OTT 조회 -->
    <select id="selectOttPlatformListByContentNo"
            parameterType="int"
            resultType="com.project.oditji.tmdb.vo.OttPlatformVO">

        SELECT
            OP.PLATFORM_NO AS platformNo,
            OP.PLATFORM_NAME AS platformName,
            OP.LOGO_IMAGE AS logoImage,
            OP.SITE_URL AS siteUrl,
            OP.IS_ACTIVE AS isActive
        FROM CONTENT_PLATFORM CP
        INNER JOIN OTT_PLATFORM OP
            ON OP.PLATFORM_NO = CP.PLATFORM_NO
        WHERE CP.CONTENT_NO = #{contentNo}
          AND OP.IS_ACTIVE = 'Y'
        ORDER BY OP.PLATFORM_NO

    </select>

</mapper>